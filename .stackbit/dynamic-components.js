/**
 * ⚠️ This file is automatically generated by @stackbit/components
 *
 * To add components using dynamic imports update the 'dynamic' property in
 * .stackbit/components-map.json
 *
 * To dynamically import a component in your code, import the 'getDynamicComponent'
 * method from '@stackbit/components/components-registry' and call it with the
 * component's registration key. The key should be the model name, which is
 * stored with every object using the 'type' field.
 *
 * Dynamically imported components are used in places where a parent component
 * can contain different types of child components, and are useful to reduce the
 * bundle size of your site pages: https://nextjs.org/docs/advanced-features/dynamic-import
 * For example, a landing page component that can contain different types of
 * section components. In this case, instead of importing a static map of all
 * available section components, effectively including them in the page bundle,
 * it is suggested to use a map of dynamic components and load each of them
 * on demand.
 */

import dynamic from 'next/dynamic';

const components = {
  'advanced': dynamic(() => import('@stackbit/components/layouts/advanced')),
  'contact_section': dynamic(() => import('@stackbit/components/components/contact-section')),
  'cta_section': dynamic(() => import('@stackbit/components/components/cta-section')),
  'hero_section': dynamic(() => import('@stackbit/components/components/hero-section')),
  'testimonials_section': dynamic(() => import('@stackbit/components/components/testimonials-section')),
  'posts_section': dynamic(() => import('@stackbit/components/components/posts-section')),
  'video_block': dynamic(() => import('@stackbit/components/components/video-block')),
  'image_block': dynamic(() => import('@stackbit/components/components/image-block')),
  'new_section': dynamic(() => import('../src/components/NewSection')),
  'zerostatic_hero': dynamic(() => import('../src/components/zerostatic/hero/Hero'))
};

export default components;
